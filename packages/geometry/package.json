{
  "name": "@buskothay/geometry",
  "version": "0.1.0",
  "private": true,
  "description": "Pure route geometry: projection, interpolation, distances. No React, no AWS, no clock.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit false --emitDeclarationOnly"
  }
}
